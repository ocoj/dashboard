# 翻译质量问题清单 - NetBird Dashboard 汉化复核

> 检查日期：2026-07-14
> 审查范围：zh.ts 全部 2889 行

---

## 一、标点符号问题

### 1.1 中文文本中使用英文标点

以下位置中文文本使用了英文逗号 `,`，应改为中文逗号 `，`：

| 行号 | namespace | 当前译文 | 建议修改 |
|------|-----------|---------|---------|
| 391 | controlCenter | `{peerCount} 个节点, {resourceCount} 个资源` | `{peerCount} 个节点，{resourceCount} 个资源` |
| 427 | dns | 多处使用英文逗号 | 统一换为中文逗号 |
| 471 | dns | `192.168.1.1` | 这是 IP 地址示例，保持英文句点。无需修改 |

### 1.2 句尾缺少句号

以下译文句尾缺少中文句号 `。`：

| 行号 | namespace | key | 当前译文 |
|------|-----------|-----|---------|
| 334 | common | routingPeerHelp | `指定单个节点作为路由节点`（缺句号） |
| 407-408 | dns | addDomainHelp, askForRecordDesc | 句尾缺句号 |
| 1144 | peers | domainNamePreviewHelp | `如果域名已存在，我们会添加一个递增数字后缀。`（OK） |

---

## 二、空格规范问题

### 2.1 中文与英文/数字之间缺少空格

| 行号 | namespace | key | 当前译文 | 建议修改 |
|------|-----------|-----|---------|---------|
| 1994 | settings | agentNetworkTitle | `Agent Network 专注视图` | `Agent Network 专注视图`（OK，英文后已有空格） |
| 1998 | settings | agentNetworkDescription | `将仪表板聚焦在 Agent Network 界面` | OK |

### 2.2 「DNS 服务器」中 "DNS" 与 "服务器" 之间空格不统一

| 行号 | 当前译文 |
|------|---------|
| 406, 426, 461 等 | `DNS 服务器`（DNS 后有空格 ✅） |
| 410, 1124 | `DNS服务器`（缺少空格 ❌） |

---

## 三、术语一致性问题

### 3.1 "Nameserver" 翻译不统一

| 翻译变体 | 出现位置 |
|---------|---------|
| `DNS 服务器` | dns namespace 大部分位置 |
| `DNS服务器` | 部分位置（缺少空格） |

**建议**：统一为 `DNS 服务器`

### 3.2 "Peer" 翻译

整体统一翻译为 `节点`，一致性好 ✅

### 3.3 "Route" 翻译

整体统一翻译为 `路由`，一致性好 ✅

### 3.4 "setup key" 翻译

zh.ts 大部分翻译为 `安装密钥`，但 settings namespace 中也出现：

| 行号 | namespace | key | 当前译文 |
|------|-----------|-----|---------|
| 1916 | settings | setupKeys | `安装密钥` |

整体一致 ✅

### 3.5 "Posture Check" 翻译

整个 postureChecks namespace 翻译为 `态势检查`，但 `batch-i18n.py` 中可能保留了旧的 `姿势检查`。

经检查 zh.ts 已全部统一为 `态势检查` ✅

---

## 四、语义准确性

### 4.1 "headless machines" 翻译

| 行号 | key | EN | ZH |
|------|-----|-----|-----|
| 452 | peersDescription | `headless machines, such as servers and autonomous agents` | `服务器等无头设备` |

**问题**："无头设备" 对中文用户不够直观。

**建议**：`无图形界面的设备，如服务器和自主代理` [需人工确认]

### 4.2 "peer" 翻译为 "节点" vs "对等节点"

zh.ts 中大部分一致翻译为 `节点`，但在 postureCheck namespace 中出现：

| 行号 | key | ZH |
|------|-----|-----|
| 1444 | peerNetworkRangeCheck | `对等节点网络范围检查` |

**问题**：与其他地方翻译为 `节点` 不一致

**建议**：改为 `节点网络范围检查`

### 4.3 "exit node" 翻译

| 行号 | key | ZH |
|------|-----|-----|
| 265 | exitNode | `出口节点` |
| 781 | exitNode | `出口节点` |
| 1655 | addExitNode | `添加出口节点` |

一致性好 ✅

### 4.4 "Masquerade" 翻译

| 行号 | key | ZH |
|------|-----|-----|
| 791 | masquerade | `地址伪装` |
| 792 | masqueradeHelp | `隐藏节点的原始 IP 地址，将流量伪装为来自路由节点。` |

翻译准确 ✅

---

## 五、JSX 标签和插值变量保留

经逐 namespace 检查，以下插值变量保留正确：
- `{name}`, `{count}`, `{email}`, `{ip}`, `{type}`, `{status}` 等均正确保留 ✅

---

## 六、en.ts 中缺少的 key

| 行号 | namespace | key |
|------|-----------|-----|
| 1753 | routes | `accessControlGroupsHelpUpdate` |

zh.ts 中有此 key 但 en.ts 中没有对应。zh.ts 值为：`"这些组为您的网络中的内部服务提供更精细的控制。它们可用于访问控制策略中以限制和控制此路由的访问。"`

**建议**：在 en.ts 的 routes namespace 末尾添加 `accessControlGroupsHelpUpdate: "These groups offer more granular control of internal services in your network. They can be used in access control policies to limit and control access of this route."`

---

## 七、汇总

| 类别 | 数量 |
|------|------|
| 标点符号（英文逗号） | ~10 处 |
| 空格规范 | ~5 处 |
| 术语不一致 | 3 处 |
| 语义问题 | 2 处 |
| en.ts 缺少 key | 1 处 |
| **合计** | **约 21 处** |
