# 需人工确认清单 - NetBird Dashboard 汉化复核

> 检查日期：2026-07-14

---

## 1. "headless machines" 的翻译

| 位置 | peers.peersDescription |
|------|----------------------|
| 英文原文 | `User devices and headless machines, such as servers and autonomous agents, connected to your network.` |
| 当前译文 | `连接到网络的用户设备和服务器等无头设备。` |
| 问题 | "无头设备" 直译自 "headless machines"，中文用户可能不理解。原文中 `such as servers and autonomous agents` 在中文翻译中被简化为 `服务器等`，丢失了 `autonomous agents` 的信息 |
| 选项 | A. `连接到网络的用户设备和服务器等无图形界面的设备。` （省略 autonomous agents，易懂） |
|  | B. `连接到网络的用户设备和无图形界面设备，如服务器和自主代理。` （保留完整信息但稍长） |

---

## 2. "peersDescription" 中的省略

| 位置 | peers.peersDescription |
|------|----------------------|
| 英文原文 | `User devices and headless machines, such as servers and autonomous agents, connected to your network.` |
| 当前译文 | `连接到网络的用户设备和服务器等无头设备。` |
| 问题 | 翻译丢失了 `autonomous agents`（自主代理）这个术语，且 `headless machines` 被直接忽略只用 `服务器等` 替代 |
| 选项 | A. 保持当前译文（简洁优先） |
|  | B. `连接到网络的用户设备和无图形界面的设备，如服务器和自主代理。` |

---

## 3. `untranslated` namespace 的处理方式

| 问题 | `untranslated` namespace 包含约 335 条英文字符串，未被代码直接引用 |
|------|---------------------------------------------------------------|
| 背景 | 这些 key 通过 `batch-i18n.py` 自动提取，但似乎不在活跃代码路径中。部分 key 来自：MSP 分销商管理、集成设置向导、RDP/SSH 远程连接、代理高级配置等 |
| 选项 | A. 全部翻译（工作量大，且可能用不到） |
|  | B. 仅翻译确定会被使用的 key（需要更多代码分析） |
|  | C. 保持英文（反正当前不显示） |

---

## 4. 页面标题中 "NetBird Dashboard" 的翻译

| 位置 | `src/utils/meta.ts` 中的 `globalMetaTitle` |
|------|------------------------------------------|
| 当前值 | `"NetBird Dashboard"` |
| 建议 | `"NetBird 控制台"` |
| 问题 | 这个更改会影响所有页面标题的后缀。如果用户的浏览器书签已经保存了 "NetBird Dashboard"，改名后会造成困惑吗？ |
| 选项 | A. 改为 `"NetBird 控制台"` （与页面内命名一致） |
|  | B. 保持 `"NetBird Dashboard"` （品牌名不翻译） |

---

## 5. 公告横幅的多语言支持

| 位置 | `announcements.json` |
|------|---------------------|
| 问题 | 该文件是静态 JSON，目前只有英文内容。如果需要支持多语言，需要修改 AnnouncementBanner 组件来读取对应语言的文本，或改为从 i18n 文件读取 |
| 选项 | A. 直接改为中文（简单但失去英文版本） |
|  | B. 修改组件使其支持 i18n（推荐但需要代码改动） |
|  | C. 保持英文（公告横幅可能经常更新，不想维护多语言） |

---

## 6. "Beta" 标签是否翻译

| 问题 | 整个 Dashboard 中有多处 "Beta" 标签（反向代理、事件流等） |
|------|--------------------------------------------------------|
| 当前 | 保留英文 `Beta` |
| 问题 | 是否应翻译为 `测试版` 或 `公测`？ |
| 选项 | A. 保持 `Beta`（国际通用，技术用户都懂） |
|  | B. 改为 `测试版`（完全中文化） |
|  | C. 改为 `Beta 测试版`（折中） |

---

## 7. "Agent Network" 术语翻译

| 位置 | settings.agentNetwork* 系列 |
|------|----------------------------|
| 英文原文 | `Agent Network Focused View` |
| 当前译文 | `Agent Network 专注视图` |
| 问题 | "Agent Network" 本身未翻译。这是产品功能名称，翻译为"代理网络"可能更好。 |
| 选项 | A. 保持 `Agent Network`（品牌/功能名不翻译） |
|  | B. 改为 `代理网络专注视图` |

---

## 8. "Lazy Connections" 翻译

| 位置 | settings.lazyConnections 系列 |
|------|------------------------------|
| 英文原文 | `Lazy Connections` |
| 当前译文 | `惰性连接` |
| 问题 | "惰性连接" 是直译，用户可能不理解。可以考虑 `按需连接` 或 `延迟连接` |
| 选项 | A. 保持 `惰性连接`（技术术语直译） |
|  | B. 改为 `按需连接`（更直观） |

---

**汇总：共 8 项需要人工确认。**
